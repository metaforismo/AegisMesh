# Continuous integration for AegisMesh.
#
# Supply-chain rules for this file:
#   - Every action is pinned to a full commit SHA (immutable), with the
#     version noted beside it for humans.
#   - Least privileges: default token permission is read-only; jobs opt in.
#   - No secrets are required for PRs; nothing here can publish artifacts.

name: ci

on:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

env:
  GOFLAGS: -mod=readonly

jobs:
  verify:
    name: fmt, vet, build, test (-race)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: gofmt + vet (+ golangci-lint when configured)
        run: |
          out=$(gofmt -l .)
          if [ -n "$out" ]; then echo "gofmt needed:"; echo "$out"; exit 1; fi
          go vet ./...

      - name: build
        run: go build -trimpath ./...

      - name: cross-build supported worker targets
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath ./...
          CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go build -trimpath ./...

      - name: test (-race)
        run: go test -race ./...

  fuzz-smoke:
    name: fuzz seeds (bounded)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      # Bounded real fuzzing (~15s/target). Post-discovery minimization is off
      # because its independent 60s default would violate this smoke bound.
      - name: fuzz targets
        run: |
          go test -run '^$' -fuzz=FuzzParseConfig         ./internal/config            -fuzztime=15s -fuzzminimizetime=0
          go test -run '^$' -fuzz=FuzzDecodeEventEnvelope ./internal/event             -fuzztime=15s -fuzzminimizetime=0
          go test -run '^$' -fuzz=FuzzMatchTCPLine        ./internal/sensor/tcpsensor  -fuzztime=15s -fuzzminimizetime=0
          go test -run '^$' -fuzz=FuzzSSHMetadataHelpers  ./internal/sensor/sshsensor  -fuzztime=15s -fuzzminimizetime=0
          go test -run '^$' -fuzz=FuzzGenerateObservation ./internal/recommend         -fuzztime=15s -fuzzminimizetime=0
          go test -run '^$' -fuzz=FuzzImportBeelzebubDoc  ./internal/migrate/beelzebub -fuzztime=15s -fuzzminimizetime=0
          go test -run '^$' -fuzz=FuzzRejectDuplicateJSONKeys ./internal/ext            -fuzztime=15s -fuzzminimizetime=0
          go test -run '^$' -fuzz=FuzzDecodeSensorProcessFrame ./internal/sensorproc     -fuzztime=15s -fuzzminimizetime=0

  helm-contract:
    name: helm chart contract
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4 # v4.3.1
        with:
          version: 'v4.2.2'

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: chart contract smoke test
        run: make helm-contract

  vulncheck:
    name: govulncheck
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: govulncheck (module mode)
        run: make vuln

  license-secrets:
    name: dependency licenses + secret scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: license policy
        run: ./scripts/license-check.sh

      - name: secret scan
        run: ./scripts/secrets-scan.sh

  supply-chain:
    name: supply-chain contracts
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: immutable reference checker
        run: make supply-chain-check

      - name: immutable reference fixtures
        run: ./scripts/check-supply-chain_test.sh

      - name: generate SBOM
        run: make sbom

      - name: validate SBOM contract
        run: make sbom-check

      - name: deterministic second generation
        run: |
          ./scripts/sbom.sh dist/sbom-aegismesh-second.cdx.json
          cmp -s dist/sbom-aegismesh.cdx.json dist/sbom-aegismesh-second.cdx.json
