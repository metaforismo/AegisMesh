apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aegismesh.fullname" . }}
  labels:
    {{- include "aegismesh.labels" . | nindent 4 }}
data:
  mesh.yaml: |
{{ include "aegismesh.meshYaml" . | indent 4 }}
